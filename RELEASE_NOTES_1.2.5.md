# Release Notes v1.2.5

## MCP 服务（Model Context Protocol）

- 内嵌 MCP 服务器（Streamable HTTP，默认关闭，Bearer 令牌鉴权）：局域网内 AI 助手（Claude、ZCode 等 MCP 客户端）可连接手机远程管理服务器。
- 20 个工具：服务器管理（状态/列表/切换/启停）、MC 控制台（命令、最多 2000 行日志）、Termux 命令执行、服务器文件管理（列/读/写/删/改名/建目录/上传/解压/导入新服务器）、模组（Modrinth 搜索/安装/列表）。
- 「更多」页新增「MCP 服务」入口：开关、端口、令牌管理、端点与客户端配置一键复制。

## FTP 文件管理

- 新增 FTP 服务（更多页入口）：电脑文件管理器直接访问手机服务器目录，支持匿名访问、账号密码、只读/可写开关、自定义端口；运行中修改设置自动重启生效。

## 内网穿透

- 新增 SakuraFrp：填入 Token 拉取隧道与节点列表、创建新隧道，选中后自动拉取官方配置启动 frpc。

## 基岩版新核心

- 新增 PocketMine-MP 与 Allay 核心（按钮样式与 PowerNukkitX 一致）：
  - PocketMine-MP：pmmp 官方通道版本（自动过滤与内置 PHP 运行时不兼容的版本），首次启动自动下载 Android 原生 PHP 运行环境；
  - Allay：GitHub Release 版本，需要 Java 21+（自动切换），首次启动自动生成配置并禁用 IPv6 绑定。

## 界面改版

- 底部导航栏与顶部状态栏区域全透明；标题栏高度增加。
- 新增「依赖与环境管理」页面（Java / 依赖 / Termux 三模块，卸载/重装/安装），概览页标题栏右侧直达入口；崩溃日志中 Java 问题可直接「前往安装」。
- 概览页移除「删除依赖」「重新安装依赖」「Java 版本管理」入口（能力收拢到管理页），保留仅在未安装时显示的「安装 Java」「安装 Termux」入口。
- 服务器管理页支持修改服务器显示名称、版本号与核心类型。

## 修复与优化

- 修复切换页面后导航状态异常重置的问题（导航状态持久化）。
- 修复 MT 管理器等外部方式复制文件夹到服务器目录后不显示的问题（自动扫描登记，降低识别门槛）。
- 文件页教程区域支持收起/展开并记住状态。
- 保活服务默认开启，应用启动 3 秒内自动激活。
- Allay/PocketMine 启动阶段识别优化，状态显示更准确。

## 其他

- 启用 R8 代码混淆与资源压缩（正式包）。
