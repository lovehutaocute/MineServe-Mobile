# MineServe Mobile 1.2.4

## 本次更新

- 服务端内存占用进一步降低：移除强制的 `-Xms` 预分配，启动即不再多占 200~500MB 无用的预留堆内存（行为对齐 EdgeCube）。
- 概览页改为 LazyColumn 滚动，全部一级卡片拆分为独立列表项，服务运行前页面掉帧明显改善。
- 日志页汉化、颜色解析和文件列表扫描移到后台线程，界面滚动更流畅。
- 打开终端页时快速加载 `latest.log` 历史日志，无需等待服务器重新输出。
- 服务器报错弹窗新增“完整日志”视图，记录服务端本次启动到停止的全部日志（含复制和分享）。
- 崩溃不再静默：运行中异常退出（含被系统终止的 exit=137）、启动后立即退出（即使退出码为 0）都会弹出错误报告。
- 崩溃信息更可靠：进程死亡时界面状态不再被错误覆盖为“运行中”；App 重新打开后 5 分钟内自动补弹最近一次崩溃报告。

## 发布信息

- Android 8.0+
- `arm64-v8a`
- Release 包已开启 R8 代码混淆和资源压缩。
- 使用原有签名文件，可覆盖安装之前使用同一签名的版本。
