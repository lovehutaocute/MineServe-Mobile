# MineServe Mobile 1.2.2

## 本次更新

- 修复应用内 CPU 占用率无法正常读取的问题，兼容 shell、PRoot 等多层启动方式。
- 服务器停止后清除旧的 CPU 采样数据，避免下次启动显示错误数值。
- 服务器正版验证开启时，概览页状态卡片显示红色提示：正版验证未关闭，离线玩家无法进入。
- 优化服务器状态、终端日志和文件列表的滚动性能，减少快速滑动时的卡顿。

## 发布信息

- Android 8.0+
- `arm64-v8a`
- Release 包已开启 R8 代码混淆和资源压缩。
- 使用原有签名文件，可覆盖安装之前使用同一签名的版本。
