本次更新以修复为主，建议 1.2.5 及更早版本升级。

【重要修复】
- 检测更新：修复发布说明为空时解析失败、导致「检测更新」完全不可用的问题
- 一键安装依赖库：补齐 libandroid-glob.so 等 19 个 Termux 依赖库（此前 APK 内一个都没打包），修复部分机型（如荣耀 Magic 6）执行 apt-get 立即崩溃、无日志、无网络的问题
- 核心版本检测：Fabric / Forge / NeoForge / Quilt 改为官方接口实时获取，此前只能看到 10 个旧版本，新版本无法显示

【新增与改进】
- 运行环境：Java / PHP 严格按用户选择生效，概览页新增「运行环境」入口
- 崩溃报告：按关键词成块提取 Watchdog、异常栈、内存错误等关键片段，不再只截取日志末尾 200 行
- 服务器配置：PowerNukkitX 按 pnx.yml 判定，配置未生成时显示引导卡片
- 界面：移除「运行诊断」卡片，诊断入口改为「服务端资源」卡片右上角的警示图标
- 发布包：固定使用发布密钥签名，老版本可直接覆盖安装

【升级提示】
- 已安装 1.2.5 及更早版本可直接覆盖安装（签名一致）
- 安装前请先备份服务器核心与世界，更新可能影响现有数据

完整更新日志见仓库 CHANGELOG.md。
